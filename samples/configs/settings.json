{
  "model": "gemini-3-flash-preview",
  "enableTerminalSandbox": false,
  "permissions": {
    "mode": "request-review",
    "allow": [
      "command(git log)",
      "command(git status)",
      "command(git diff)",
      "command(npm test)",
      "command(npm run lint)"
    ]
  }
}
